[pytest]
# Pytest configuration

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Test paths
testpaths = tests

# Output options
addopts =
    -v
    --strict-markers
    --tb=short
    --disable-warnings
    --color=yes
    
# Coverage options (if pytest-cov is installed)
# --cov=core
# --cov=ml_engine
# --cov-report=term-missing
# --cov-report=html:htmlcov

# Markers
markers =
    unit: Unit tests (fast, no external dependencies)
    integration: Integration tests (may require external services)
    slow: Slow tests (may take several seconds)
    gpu: Requires CUDA; auto-skipped in CI by tests/conftest.py when unavailable

# pytest-asyncio: explicit mode means every async test must carry
# @pytest.mark.asyncio. This is the safer default — auto mode silently
# promotes any async def test to an asyncio test, which can mask fixture
# scope mismatches. Existing tests already use @pytest.mark.asyncio, so
# explicit is correct.
asyncio_mode = strict
    
# Ignore paths
norecursedirs = 
    .git
    .tox
    dist
    build
    *.egg
    __pycache__
    deps
    GroundingDINO
    segment_anything
    EfficientSAM
    experiments
    logs
    output

# Logging
log_cli = false
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)8s] %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

# Warnings
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
